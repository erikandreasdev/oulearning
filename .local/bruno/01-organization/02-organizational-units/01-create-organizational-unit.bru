meta {
  name: Create Organizational Unit
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/v1/organizational-units
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "name": "{{ouName}}"
  }
}

vars:post-response {
  ouId: res.body.id
}
