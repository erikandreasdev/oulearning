# Production-quality only

- Every solution must be production-ready. No development-only workarounds, bootstrapping shortcuts, or "good enough for now" hacks.
- If a proper production solution requires more code or configuration, write it. Never defer it to a "future task" or suggest it as an improvement.
- Infrastructure configuration (datasources, migrations, caching, messaging) must use the production-grade approach from the start.
