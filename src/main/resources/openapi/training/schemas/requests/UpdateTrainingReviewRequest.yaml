type: object
required:
  - comments
  - modality
  - startDate
  - endDate
  - reviewedAt
properties:
  comments:
    type: string
  modality:
    type: string
  startDate:
    type: string
    format: date-time
  endDate:
    type: string
    format: date-time
  externalProviderId:
    type: integer
    format: int64
  reviewedAt:
    type: string
    format: date-time
