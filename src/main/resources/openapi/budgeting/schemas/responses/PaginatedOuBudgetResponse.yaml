type: object
required:
  - items
  - totalElements
  - totalPages
  - page
  - size
properties:
  items:
    type: array
    items:
      $ref: './OuBudgetResponse.yaml'
  totalElements:
    type: integer
    format: int64
  totalPages:
    type: integer
    format: int32
  page:
    type: integer
    format: int32
  size:
    type: integer
    format: int32
